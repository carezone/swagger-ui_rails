diff --git a/src/main/template/param.handlebars b/src/main/template/param.handlebars
index e38d53d..77e8af9 100644
--- a/src/main/template/param.handlebars
+++ b/src/main/template/param.handlebars
@@ -8,11 +8,11 @@
                {{else}}
                        {{#if default}}
                                <div class="editor_holder"></div>
-                               <textarea class='body-textarea' name='{{{escape name}}}' id='{{{escape valueId}}}'>{{{escape default}}}</textarea>
+                               <textarea data-threescale-name='{{threescale_name}}' class='body-textarea' name='{{{escape name}}}' id='{{{escape valueId}}}'>{{{escape default}}}</textarea>
         <br />
         <div class="parameter-content-type" />
                        {{else}}
-                               <textarea class='body-textarea' name='{{{escape name}}}' id='{{{escape valueId}}}'></textarea>
+                               <textarea data-threescale-name='{{threescale_name}}' class='body-textarea' name='{{{escape name}}}' id='{{{escape valueId}}}'></textarea>
                                <div class="editor_holder"></div>
                                <br />
                                <div class="parameter-content-type" />
@@ -20,7 +20,7 @@
                {{/if}}
        {{else}}
                {{#if isFile}}
-                       <input type="file" name='{{{escape name}}}' id='{{{escape valueId}}}'/>
+                       <input data-threescale-name='{{threescale_name}}' type="file" name='{{{escape name}}}' id='{{{escape valueId}}}'/>
                        <div class="parameter-content-type" />
                {{else}}
                        {{#renderTextParam this}}
